update readme file.

automate user tests. in github action, run command shcedule 1/2 min, then aws command retrieve event result last one before end, verify it exists, then wait 1 min, then retrieve newest codebuild job, and check its status. 

discuss test vpc, its traffic data flow. vpc test: put the bash code in vpc test in separate files. move the test relted files into folder test/ discuss vpc test.

discuss subscribed users and cancelled-subscription accessibility.

make check for git repo and config variables defined at begining of program run. gits.cpp: duplicate check of current git repo. factorise gits.cpp into multiple files like Helix cli.

make delete cmd deschedule. fix the names of components/modules mess.

test on macOS

add support to gitlab.